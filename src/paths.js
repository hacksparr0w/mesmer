const Paths = ({
  projectDirectoryPath,
  buildDirectoryPath,
  configFilePath,
  clientBundleFilePath,
  serverBundleFilePath,
  metadataFilePath
}) => ({
  projectDirectoryPath,
  buildDirectoryPath,
  configFilePath,
  clientBundleFilePath,
  serverBundleFilePath,
  metadataFilePath
});

export {
  Paths
};
